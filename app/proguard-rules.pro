# Shrinking / obfuscation rules for release builds.
# Not enabled by default in this project (see build.gradle.kts buildTypes.release.isMinifyEnabled).
# If you turn on minification, add rules here for any libraries that need them.
